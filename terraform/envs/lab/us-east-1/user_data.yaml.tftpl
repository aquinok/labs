#cloud-config
users:
  - name: ubuntu
    lock_passwd: false
    passwd: ${ubuntu_password_hash}
    shell: /bin/bash
    sudo: ["ALL=(ALL) ALL"]
    ssh_authorized_keys:
      - ${ssh_public_key}

ssh_pwauth: false
disable_root: true
