all:
  vars:
    ansible_user: ubuntu
    ansible_python_interpreter: /usr/bin/python3
    ansible_ssh_private_key_file: ${SSH_KEY}
    zabbix_server_private_ip: ${ZABBIX_PRIVATE_IP}
    zabbix_api_url: "http://${ZABBIX_PUBLIC_IP}/zabbix/api_jsonrpc.php"

  children:
    zabbix:
      hosts:
${ZABBIX_HOST_BLOCK}

    vault:
      hosts:
${VAULT_HOSTS_BLOCK}
